NOVARA FIX - IMPORTANT

1. Do NOT delete or replace your existing static/icons/ folder.
   Keep all your existing icon files there, especially icon-72.png and icon-192.png.
2. Replace app.py and the listed static/templates files with these versions.
3. In Render Environment Variables, set:
   POLLINATIONS_API_KEY = your server-side secret key
   Optional: POLLINATIONS_IMAGE_MODEL = nanobanana-2
   Optional: POLLINATIONS_EDIT_MODEL = kontext
4. The image generator now uses the current authenticated Pollinations API and no longer falls back to the old legacy image endpoint.
5. For exact real-person likeness, image models can still vary; a reference image is the most reliable route when the provider supports image-to-image.
